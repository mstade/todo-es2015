# ES2015 sample todo app

First install dependencies:

```bash
$ npm install
```

To just run the app:

```bash
$ npm run start
```

To start the app, and compile source on changes:

```bash
$ npm run start:dev
```